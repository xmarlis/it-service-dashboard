/**
 * Polyfills for the application.
 * Zone.js is required by Angular for change detection.
 * Keep this file minimal — the CLI will include it in builds/tests.
 */

import 'zone.js';

// For tests that require additional testing zone patching, the testing setup
// (if present) should import 'zone.js/testing'.
